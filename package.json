{
  "name": "mnemosyne",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter @mnemosyne/api dev",
    "start": "pnpm --filter @mnemosyne/api start",
    "demo": "pnpm --filter @mnemosyne/example-agent demo",
    "dev:public": "./scripts/dev-public.sh",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
