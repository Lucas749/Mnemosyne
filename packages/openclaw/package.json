{
  "name": "@mnemosyne/openclaw",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "description": "OpenClaw MemoryAdapter — plug Mnemosyne into any OpenClaw agent as its persistent memory brain",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@mnemosyne/compute": "workspace:*",
    "@mnemosyne/storage": "workspace:*",
    "@mnemosyne/types": "workspace:*"
  },
  "devDependencies": {
    "dotenv": "^16.0.0",
    "typescript": "^5.0.0",
    "vitest": "^1.0.0"
  }
}
